# NewsFeedsUIDemo
NewsFeedsUIDemo



if cocoapods not installed, run `gem install cocoapods`

if cocoapods-keys not installed, run`gem install cocoapods-keys`

then

run `pod install`

you will be asked to input app key and secret which you will find in youliao.163yun.com